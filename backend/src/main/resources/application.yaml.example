spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    driverClassName: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_example
    username: springuser
    password: ThePassword
lombok:
  accessors:
    fluent: "true"
    chain: "true"

---

spring:
	profiles: test
server:
	port: 9001

---

spring:
	profiles: prod
server:
	port: 0